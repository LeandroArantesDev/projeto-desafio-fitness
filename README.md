# projeto-desafio-fitness

## Como rodar

Subir os containers (MySQL + Tomcat):

```bash
docker compose up -d
```

Parar os containers:

```bash
docker compose down
```

Gerar o build do projeto:

```bash
mvn clean package
```
